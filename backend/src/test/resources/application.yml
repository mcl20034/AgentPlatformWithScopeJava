spring:
  datasource:
    url: jdbc:h2:mem:agent_platform;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  session:
    jdbc:
      initialize-schema: never
server:
  servlet:
    session:
      cookie:
        secure: false
platform:
  secrets:
    key-id: test-v1
    master-key: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
  network:
    allowed-hosts:
      - localhost
      - 127.0.0.1
  storage:
    root: target/test-data
  bootstrap:
    admin-username: admin
    admin-password: Initial-Admin-2026!
  security:
    bcrypt-strength: 4
