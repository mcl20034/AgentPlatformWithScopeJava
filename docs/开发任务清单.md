# 开发任务清单

## 迭代 1：基础工程与认证闭环

- [x] Spring Boot 3.5.16 / Java 21 后端工程。
- [x] Vue 3 / TypeScript / Vite / Vue Router / Pinia / Element Plus 前端工程。
- [x] PostgreSQL Flyway 初始迁移和 Spring Session JDBC。
- [x] 一次性管理员初始化，不设置固定默认密码。
- [x] BCrypt 密码、CSRF、HttpOnly/SameSite Cookie 和同源接口。
- [x] 登录、退出、当前账号、首次强制改密。
- [x] 管理员创建账号、临时密码仅返回一次、重置密码、启停账号。
- [x] 后端角色授权和前端路由守卫。
- [x] 登录、改密、应用外壳、问答空页面、账号、403、404 页面。
- [x] 认证主链路集成测试和前端生产构建。

对应技术设计第 2、3、10、11、13、16 节；覆盖 PRD 权限基础要求，为 AC-08、AC-10 提供第一批实现。

## 迭代 2：管理员配置链路

- [x] 密钥服务和 AES-GCM 密文版本。
- [x] 对话模型与 Embedding 模型配置、连接测试、版本基线和启停门槛。
- [x] MySQL 数据源连接、结构刷新、表级授权、业务指标口径和发布记录；按最新需求不校验账号是否仅有只读权限。
- [x] Elasticsearch 单索引连接、mapping 刷新、字段能力推导、授权范围和发布检查。
- [x] 知识库目录、Embedding 关联、PDF/DOCX/TXT 上传、内容去重、版本、异步解析、失败重试和索引代次边界。

第二迭代按当前环境完成。由于用户已明确暂缓安装标准 PostgreSQL + pgvector，重建索引会建立 `BLOCKED / VECTOR_STORE_UNAVAILABLE` 代次并明确返回原因，不会伪造可检索状态；安装完成后继续实现向量写入、验证和原子切换。

## 迭代 3：智能问答最小闭环

- [x] 已启用对话模型和已发布 MySQL 数据源选项。
- [x] 会话、消息、分析任务和执行阶段迁移。
- [x] 新建会话、首问命名及会话内模型和数据源锁定。
- [x] 任务受理、单会话互斥和全局并发名额；请求幂等键后续补充。
- [x] SSE 进度事件、断线轮询降级和前端结果恢复。
- [x] OpenAI 兼容服务与 Ollama 对话调用。
- [x] MySQL 单数据源 SQL 生成、表级授权校验、只读连接、15 秒超时和 200 行上限。
- [x] 自然语言答案、SQL 和结构化结果展示。
- [x] 服务重启后将遗留运行中任务安全收敛为失败，并提供可恢复的任务事件历史。
- [ ] AgentScope 适配层。

## 迭代 4：查询、证据与导出

- [x] 使用 JSqlParser 将第三迭代的保守 SQL 规则升级为 MySQL AST 安全网关。
- [x] Elasticsearch DSL 字段级安全网关、200 条硬上限与查询执行。
- [x] Elasticsearch 字段语义目录、只读样本采集、嵌套 mapping 展开和用途字段配置。
- [x] Elasticsearch 问答改为“模型查询计划 → 后端生成 DSL → 安全网关”链路。
- [ ] PostgreSQL + pgvector 就绪后接入文档检索。
- [x] 查询证据抽屉、结构化结果、简单图表、CSV 和 Markdown 导出。
- [x] 主动停止、查询超时、服务重启收敛和全局并发控制。
- [x] 跨来源引用一致性校验与知识库检索部分失败结果编排。
- [x] 按 Stitch 原型重构智能问答为会话、报告和执行审计三栏工作区。

## 迭代 5：知识库检索与多来源问答

- [x] Flyway V6 文档片段、索引代次统计和会话知识库关联模型。
- [x] PostgreSQL pgvector 扩展检查、文档切分、批量 Embedding 写入和索引代次原子切换。
- [x] 知识库管理页面、文档上传、解析状态、索引重建和启停。
- [x] 会话同时选择一个结构化数据源和最多五个知识库。
- [x] 向量召回、文档/页码/片段引用、证据抽屉和 Markdown 引用导出。
- [x] 无效引用清理以及知识库检索失败时保留结构化查询结果的部分失败处理。
- [ ] 在用户提供的真实 PostgreSQL + pgvector 和 Embedding 服务上完成端到端验收。
